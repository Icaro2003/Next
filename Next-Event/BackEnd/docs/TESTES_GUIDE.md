# 🧪 Guia de Testes - Next-Event API\n\n## 📋 Visão Geral\n\nEste projeto implementa uma suite completa de testes unitários e de integração para garantir a qualidade e confiabilidade da API Next-Event.\n\n## 🗂️ Estrutura de Testes\n\n`\ntests/\n├── setup/\n│   └── jest.setup.ts           # Configuração global do Jest\n├── helpers/\n│   └── TestDataBuilder.ts      # Builders e mocks para testes\n├── unit/\n│   ├── application/\n│   │   └── user/\n│   │       └── BolsistaViewDataUseCase.spec.ts\n│   ├── infrastructure/\n│   │   └── aluno/\n│   │       └── PostgresAlunoRepository.spec.ts\n│   └── presentation/\n│       └── aluno/\n│           └── AlunoController.spec.ts\n└── integration/\n    └── api/\n        ├── aluno/\n        │   └── alunoRoutes.spec.ts\n        ├── bolsista/\n        │   └── bolsistaRoutes.spec.ts\n        └── health/\n            └── healthCheck.spec.ts\n`\n\n## 🛠️ Configuração\n\n### Pré-requisitos\n\n- Node.js 18+\n- PostgreSQL (para testes de integração)\n- Variáveis de ambiente configuradas\n\n### Variáveis de Ambiente para Testes\n\nCrie um arquivo `.env.test` (opcional) ou configure:\n\n`bash\nDATABASE_TEST_URL=\"postgresql://postgres:password@localhost:5432/nextevent_test\"\n`\n\n### Configuração do Jest\n\nO Jest está configurado com:\n\n- **Preset**: `ts-jest` para suporte TypeScript\n- **Environment**: `node` para APIs Node.js\n- **Setup**: Configuração automática de banco e mocks\n- **Projects**: Separação entre unit e integration tests\n- **Coverage**: Relatórios de cobertura detalhados\n\n## 🚀 Executando os Testes\n\n### Comandos Principais\n\n`bash\n# Todos os testes\nnpm test\n\n# Apenas testes unitários\nnpm run test:unit\n\n# Apenas testes de integração\nnpm run test:integration\n\n# Testes com cobertura\nnpm run test:coverage\n\n# Modo watch (desenvolvimento)\nnpm run test:watch\n\n# CI/CD (sem watch)\nnpm run test:ci\n`\n\n### Executando Testes Específicos\n\n`bash\n# Testes de um arquivo específico\nnpm test -- AlunoController.spec.ts\n\n# Testes com padrão no nome\nnpm test -- --testNamePattern=\"deve criar\"\n\n# Testes de um diretório\nnpm test -- tests/unit/application/\n`\n\n## 🎯 Tipos de Testes\n\n### 1. Testes Unitários\n\n**Localização**: `tests/unit/`\n\n**Objetivo**: Testar componentes isoladamente\n\n**Cobertura**:\n- ✅ Use Cases (lógica de negócio)\n- ✅ Controllers (handlers HTTP)\n- ✅ Repositories (acesso a dados)\n- ✅ Entities (domínio)\n\n**Exemplo**:\n`typescript\ndescribe('CreateAlunoUseCase', () => {\n  let useCase: CreateAlunoUseCase;\n  let mockRepository: jest.Mocked<IAlunoRepository>;\n\n  beforeEach(() => {\n    mockRepository = MockFactory.createAlunoRepositoryMock();\n    useCase = new CreateAlunoUseCase(mockRepository);\n  });\n\n  it('deve criar aluno com dados válidos', async () => {\n    // Arrange, Act, Assert\n  });\n});\n`\n\n### 2. Testes de Integração\n\n**Localização**: `tests/integration/`\n\n**Objetivo**: Testar fluxos completos da API\n\n**Cobertura**:\n- ✅ Endpoints HTTP\n- ✅ Banco de dados real\n- ✅ Middleware de autenticação\n- ✅ Validações de entrada\n- ✅ Respostas completas\n\n**Exemplo**:\n`typescript\ndescribe('Aluno API', () => {\n  beforeEach(async () => {\n    await cleanDatabase();\n    await seedTestData();\n  });\n\n  it('POST /api/alunos - deve criar aluno', async () => {\n    const response = await request(app)\n      .post('/api/alunos')\n      .set('Authorization', authToken)\n      .send(alunoData)\n      .expect(201);\n    \n    expect(response.body.data).toHaveProperty('id');\n  });\n});\n`\n\n## 🧰 Utilitários de Teste\n\n### TestDataBuilder\n\nClasse para criar dados de teste consistentes:\n\n`typescript\n// Criar entidades de teste\nconst usuario = TestDataBuilder.createUsuario();\nconst curso = TestDataBuilder.createCurso();\nconst aluno = TestDataBuilder.createAluno({\n  usuarioId: usuario.id,\n  cursoId: curso.id\n});\n\n// Criar mocks de repository\nconst mockRepo = MockFactory.createAlunoRepositoryMock();\n\n// Criar mocks Express\nconst req = ExpressMocks.createMockRequest();\nconst res = ExpressMocks.createMockResponse();\n`\n\n### Setup Automático\n\nO `jest.setup.ts` configura automaticamente:\n- Conexão com banco de teste\n- Limpeza entre testes\n- Mocks de console\n- Variáveis globais\n\n## 📊 Cobertura de Código\n\n### Relatórios de Cobertura\n\n`bash\n# Gerar relatório de cobertura\nnpm run test:coverage\n\n# Abrir relatório HTML\nopen coverage/lcov-report/index.html\n`\n\n### Metas de Cobertura\n\n- **Statements**: 80%+\n- **Branches**: 75%+\n- **Functions**: 85%+\n- **Lines**: 80%+\n\n### Arquivos Excluídos\n\n- `src/main.ts` (bootstrap)\n- `src/**/*Config.ts` (configuração)\n- `src/infrastructure/database/**` (Prisma generated)\n- `src/**/*.d.ts` (type definitions)\n\n## 🔧 Melhores Práticas\n\n### Estrutura de Teste\n\n`typescript\ndescribe('ComponentName', () => {\n  // Setup\n  let component: ComponentType;\n  let dependencies: MockType;\n\n  beforeEach(() => {\n    // Initialize for each test\n  });\n\n  describe('methodName', () => {\n    it('deve comportamento esperado quando condição', async () => {\n      // Arrange\n      const input = createTestData();\n      \n      // Act\n      const result = await component.method(input);\n      \n      // Assert\n      expect(result).toEqual(expectedOutput);\n      expect(mockDependency).toHaveBeenCalledWith(expected);\n    });\n  });\n});\n`\n\n### Naming Conventions\n\n- **Describes**: Nome do componente/método sendo testado\n- **Its**: \"deve [comportamento] quando [condição]\"\n- **Variáveis**: Nomes descritivos e em português\n- **Files**: `ComponentName.spec.ts`\n\n### Isolamento de Testes\n\n`typescript\n// ✅ Bom - cada teste independente\nbeforeEach(() => {\n  mockRepo.reset();\n});\n\n// ❌ Evitar - estado compartilhado\nconst sharedData = { /* ... */ };\n`\n\n### Dados de Teste\n\n`typescript\n// ✅ Usar builders\nconst aluno = TestDataBuilder.createAluno({\n  tipoAcesso: TipoAcessoAluno.ACESSO_TUTOR\n});\n\n// ❌ Dados hardcoded\nconst aluno = {\n  id: 'aluno-123',\n  nome: 'João Silva',\n  // ...\n};\n`\n\n## 🚨 Solução de Problemas\n\n### Problemas Comuns\n\n#### 1. Timeout nos testes\n`bash\n# Aumentar timeout no jest.config.js\ntestTimeout: 15000\n\n# Ou por teste\nit('teste longo', async () => {\n  // ...\n}, 20000);\n`\n\n#### 2. Banco de dados não encontrado\n`bash\n# Verificar se PostgreSQL está rodando\npsql -U postgres\n\n# Criar banco de teste\nCREATE DATABASE nextevent_test;\n`\n\n#### 3. Tipos Jest não encontrados\n`bash\n# Verificar se @types/jest está instalado\nnpm install --save-dev @types/jest\n\n# Restart TypeScript server no VS Code\nCtrl+Shift+P → \"TypeScript: Restart TS Server\"\n`\n\n### Debugging\n\n`typescript\n// Debug específico\nit.only('teste específico', () => {\n  // Só este teste rodará\n});\n\n// Skip temporário\nit.skip('teste com problema', () => {\n  // Teste será ignorado\n});\n\n// Debug com console\nit('teste debug', () => {\n  console.log('Debug info:', data);\n  expect(data).toBeDefined();\n});\n`\n\n## 📈 Métricas e Monitoramento\n\n### CI/CD Integration\n\n`bash\n# No pipeline de CI\nnpm run test:ci\n\n# Com reporting para Sonar/CodeCov\nnpm run test:coverage -- --coverageReporters=lcov\n`\n\n### Performance dos Testes\n\n`bash\n# Executar com verbose para timings\nnpm test -- --verbose\n\n# Detectar testes lentos\nnpm test -- --detectSlowTests\n`\n\n## 📚 Recursos Adicionais\n\n- [Jest Documentation](https://jestjs.io/docs/getting-started)\n- [Supertest Guide](https://github.com/visionmedia/supertest)\n- [Testing Node.js Applications](https://nodejs.org/en/docs/guides/testing/)\n- [Prisma Testing Guide](https://www.prisma.io/docs/guides/testing)\n\n---\n\n**Status**: ✅ Suite de testes completa implementada\n\n**Última atualização**: Janeiro 2026\n
